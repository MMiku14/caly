[package]
name = "caly-server"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[package.metadata.caly]
# docs/crate-replan.md v4:层序坐标;依赖只允许 高 layer -> 低 layer。
axis = "role"
layer = 5

[dependencies]
caly-domain.workspace = true
caly-application.workspace = true
caly-protocol.workspace = true
tokio.workspace = true
tokio-rustls.workspace = true
rustls-pemfile.workspace = true
tokio-stream.workspace = true
tracing.workspace = true

[dev-dependencies]
# Test-only: shared `unique_path_under` helper for hermetic temp paths.
# Production code never reaches this crate through `caly-server`.
caly-platform.workspace = true

[lints]
workspace = true
